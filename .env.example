# Comma-separated browser origins allowed to call this API with credentials.
# Required in production when the frontend is on a different domain than the API.
# Example: FRONTEND_ORIGIN=https://your-frontend.vercel.app
FRONTEND_ORIGIN=http://localhost:5173

DATABASE_URL=
SESSION_SECRET=
